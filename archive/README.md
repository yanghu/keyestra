# Archive

This folder is for local reference material that should not be treated as active
project source.

## Local

`archive/local/` is ignored by Git. It currently holds the legacy
`sightreading.training` web project that was previously sitting at the repository
root. The active FP-10 monitor web UI is embedded in
`src/bin/fp10-monitor-server.rs`.
