# Archived velocity curves

Files in this directory are retained only as historical references. Keyestra
does not package or select them. The main CLP curve is `examples/curve.toml`;
the retained top-linear alternative is `examples/curve-top-linear.toml`.
